# 大宗商品轮动模型使用说明

## 1. 模型定位

- 频率：周频（每周五收盘后调仓，下一周收益生效）
- 方向：多头轮动（从四个商品中选 TopN）
- 标的：期货主力连续近似（黄金、白银、铜、布伦特原油）
- 实盘映射：输出 ETF 代理持仓建议

## 2. 运行方式

在当前目录执行：

```bash
python3 rotation_model.py
```

输入文件默认读取：

- `大宗商品轮动_数据2.xlsx`

## 3. 策略框架（已实现）

- 数据层：读取价格与因子子表，统一到周频（`W-FRI`）
- 信号层：
  - 动量：`12-1` 周动量
  - 基本面：实际利率、美元指数、PMI、库存/持仓变化的组合
  - 截面标准化：对每周四个品种做 z-score
  - 综合评分：`0.6 * 动量 + 0.4 * 基本面`
- 组合层：
  - 每周选 TopN（默认 2）
  - 等权持有，约束单品种上限、行业权重上限、最大换手
- 交易层：
  - 成本 = 换手 * 成本bps（默认 15bps）
  - 生成净值、回撤、Sharpe、Calmar、胜率等指标

## 4. 输出结果目录

运行完成后会生成 `model_outputs/`，包括：

- `weekly_panel.csv`：周频价格/收益/因子/评分面板
- `weights.csv`：每周组合权重、换手、成本、入选品种
- `strategy_returns.csv`：策略周收益
- `nav_curve.csv`：策略净值曲线
- `performance_summary.csv`：全样本、样本内、样本外绩效
- `annual_returns.csv`：年度收益
- `drawdown_periods.csv`：主要回撤区间
- `etf_weights.csv`：ETF代理持仓建议
- `param_sensitivity.csv`：参数敏感性（TopN、动量窗口）
- `robustness_tests.csv`：鲁棒性（成本、单因子情形）

## 5. 参数入口

在 `rotation_model.py` 的 `BacktestConfig` 中可调整：

- `top_n`
- `momentum_lookback_weeks`
- `cost_bps`
- `max_weight_per_asset`
- `max_weight_per_sector`
- `max_turnover`
- `score_momentum_weight`
- `score_fundamental_weight`
- `start_date`

## 6. 注意事项

- 当前 ETF 映射为规则映射，建议后续接入流动性和规模过滤。
- 若新增更多商品，可在 `ASSETS`、`SECTOR_MAP` 与因子构造处扩展。
- 若 Wind 子表命名变化，可在 `load_weekly_factors` 的候选表名中补充。
